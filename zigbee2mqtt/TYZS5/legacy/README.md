# REFERENCE / LEGACY — DO NOT USE

kagel_lock.js is retained unchanged for historical reference only. It contains old identity and control/OTA behavior that is not the TYZS5 production policy.
Do not copy it into active external_converters or load it alongside the canonical ../tuya_ty0a01_tyzs5.js.

Its original SmartHomePlus / LCK-BI400 identity is also not the current TZLL TYZS3 identity. For TYZS3 use `../../TYZS3/tuya_ty0a01_tyzs3.js` (Tuya / TY0A01-TYZS3). Never load multiple definitions that match the same legacy identity.
